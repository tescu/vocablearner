FEATURES:
x (WEB) assure desktop and mobile compatibility
x (WEB) light and dark themes (via css media flag)
- (CLI/WEB) 'reverse' game mode; guess English words using target language
- (CLI/WEB) different scoring methods?
	There should be some sort of motivational factor that makes the user not
	want to make mistakes.
	- (currently) score goes up infinitely; game continues until user gives up
	- idea: score goes up infinitely; game stops when user makes a mistake N times
		- grant an extra life every 5-10 words guessed right?
- (CLI/WEB) export to scoreboard
- (CLI/WEB) load custom dictionaries inside program? 
- (CLI) install & uninstall script
- (CLI) configuration via environmental vars
